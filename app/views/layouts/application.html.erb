<!DOCTYPE html>
<html>
  <head>
    <title>ParentingSupportApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen flex flex-col">
    <%= render "layouts/header" %>

    <% if notice %>
      <p class="bg-amber-100 text-amber-800 text-center py-2 px-4"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="bg-rose-100 text-rose-800 text-center py-2 px-4"><%= alert %></p>
    <% end %>

    <main class="container mx-auto mt-10 px-5 flex flex-1">
      <%= yield %>
    </main>
    <%= render "layouts/footer" %>
  </body>
</html>
